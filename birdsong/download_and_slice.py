"""
This script is supposed to download a list of recording IDs from Xeno-Canto,
store them in a temporary folder, extract the signal timestamps and then prepare
all spectrogram slices and pickle them, storing every done job in the sqlite
database, then deleting the orignal file. """
